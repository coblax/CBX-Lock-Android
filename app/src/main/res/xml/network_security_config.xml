<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Enforce HTTPS-only connections for public domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow HTTP (cleartext) for local/intranet exam servers.
         Many schools self-host exam servers on internal networks using plain
         HTTP (e.g., http://192.168.1.100/ujian). Without these exceptions,
         the WebView shows "ERR_CLEARTEXT_NOT_PERMITTED" which students see
         as "Koneksi Terputus" even though the network is fine. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
